<div class="alert alert-danger mb-0" {{ attributes }}>
    {{ form_start(form, {
        attr: {
            class: 'form-horizontal',
            'data-model': 'norender|*',
            'data-action': 'live#action:prevent',
            'data-live-action-param': 'save'
        }
    }) }}
        <div class="input-group">
            <span class="input-group-text" style="width:200px">{{ 'header.account'|trans }}</span>
            {{ form_widget(form.service, {attr: {class: 'form-select'}}) }}
            {{ form_widget(form.year, {attr: {class: 'form-select'}}) }}
            {{ form_widget(form.month, {attr: {class: 'form-select'}}) }}
            {{ form_widget(form.amount, {attr: {placeholder: '0.00'}}) }}
            <button type="submit" class="btn btn-primary">{{ 'action.add'|trans }}</button>
        </div>
        <div class="text-danger">
            {{ form_errors(form) }}
        </div>
        <input type="hidden" name="placeId" value="{{ placeId }}">
    {{ form_end(form) }}
</div>
