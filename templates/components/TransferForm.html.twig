<div class="alert alert-primary" {{ attributes }} data-loading="addClass(opacity-50) addAttribute(disabled)">
    {{ form_start(form, {
        attr: {
            class: 'form-horizontal',
            'data-model': 'norender|*',
            'data-action': 'live#action:prevent',
            'data-live-action-param': 'save'
        }
    }) }}
        <div class="input-group">
            <span class="input-group-text" style="width:125px">{{ 'Transfer'|trans }}</span>
            {{ form_widget(form.cardOut, {attr: {class: 'form-select'}}) }}
            {{ form_widget(form.cardIn, {attr: {class: 'form-select'}}) }}
            {{ form_widget(form.date) }}
            {{ form_widget(form.balance, {attr: {placeholder: '0.00'}}) }}
            <button type="submit" class="btn btn-primary">{{ 'Add'|trans }}</button>
        </div>
        <div class="text-danger">
            {{ form_errors(form) }}
        </div>
    {{ form_end(form) }}
</div>
