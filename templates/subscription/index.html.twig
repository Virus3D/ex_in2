<details class="card mb-3">
    <summary class="card-header">
        {{ 'Subscriptions'|trans }}
    </summary>
    <div class="card-body">
        <turbo-frame id="subscription_frame" src="{{ path('app_subscription_view') }}"></turbo-frame>
        <turbo-frame id="subscription_form_frame" src="{{ path('app_subscription_form') }}"></turbo-frame>
    </div>
</details>