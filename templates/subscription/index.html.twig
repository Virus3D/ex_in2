<details class="card mb-3">
    <summary class="card-header">
        {{ 'header.subscriptions'|trans }}
    </summary>
    <div class="card-body">
        {{ component('SubscriptionList') }}
        {{ component('SubscriptionForm') }}
    </div>
</details>